import 'package:flutter/material.dart';

class EchangePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) => Scaffold(
        //drawer: NavigationDrawerWidget(),
        appBar: AppBar(
          title: Text('Echanges'),
          centerTitle: true,
          backgroundColor: Colors.green,
        ),
      );
}